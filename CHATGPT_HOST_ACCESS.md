# 🔐 ChatGPT-CLI: Доступ к хосту с root-правами

## ✅ Применённые изменения

### 1. docker-compose.yml

Добавлены следующие настройки безопасности для контейнера `chatgpt-cli`:

```yaml
chatgpt-cli:
  privileged: true              # Привилегированный режим
  pid: host                     # Доступ к PID namespace хоста
  cap_add:                      # Дополнительные capabilities
    - SYS_ADMIN                 # Для nsenter
    - SYS_PTRACE                # Для отладки/трассировки
    - NET_ADMIN                 # Сетевое администрирование
  security_opt:
    - apparmor:unconfined       # Отключение AppArmor
```

### 2. Dockerfile

Пользователь `chatgpt` получил sudo-права без пароля:

```dockerfile
RUN echo "chatgpt ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers
```

---

## 🚀 Как применить изменения

### Шаг 1: Пересобрать образ

```bash
cd /home/claude/workspace
sudo docker compose build chatgpt-cli
```

### Шаг 2: Перезапустить контейнер

```bash
sudo docker compose down chatgpt-cli
sudo docker compose up -d chatgpt-cli --profile chatgpt
```

Или если профиль уже активен:

```bash
sudo docker compose restart chatgpt-cli
```

---

## 🔧 Использование host-exec

После перезапуска контейнера будет доступна команда `host-exec` для выполнения команд на хосте:

### Примеры использования:

```bash
# Проверка сетевой связности на хосте
host-exec ping -c 4 8.8.8.8

# Просмотр процессов хоста
host-exec ps aux

# Запуск команды с правами root на хосте
host-exec sudo systemctl status docker

# Просмотр сетевых интерфейсов хоста
host-exec ip addr show

# Управление Docker на хосте
host-exec docker ps
host-exec docker logs <container-name>
```

### Как работает host-exec:

1. Использует `nsenter` для входа в namespace хоста
2. Выполняет команды в контексте процесса init (PID 1) хоста
3. Автоматически использует `sudo` если запущен не от root

---

## ⚠️ Важные замечания по безопасности

### Риски:

1. **Полный доступ к хосту**: Контейнер имеет те же права, что и root на хосте
2. **Обход изоляции**: Нарушается основная концепция контейнеризации
3. **Потенциальная уязвимость**: Компрометация контейнера = компрометация хоста

### Рекомендации:

- ✅ Используйте только в trusted/контролируемых окружениях
- ✅ Не предоставляйте доступ к этому контейнеру ненадёжным пользователям
- ✅ Регулярно обновляйте образ и зависимости
- ✅ Мониторьте логи контейнера на подозрительную активность
- ✅ Рассмотрите использование менее привилегированных альтернатив для production

### Альтернативы для production:

1. **Docker Socket** - монтирование `/var/run/docker.sock` (только для Docker API)
2. **SSH** - подключение к хосту через SSH (более безопасно, но требует настройки)
3. **API Gateway** - создание защищённого API для нужных операций
4. **Kubernetes** - использование ServiceAccount с ограниченными RBAC правами

---

## 🧪 Проверка функциональности

После перезапуска контейнера проверьте работу:

```bash
# Войти в контейнер
sudo docker exec -it chatgpt-cli bash

# Проверить доступность host-exec
which host-exec

# Проверить sudo-права
sudo -l

# Тест host-exec
host-exec echo "Hello from host!"

# Проверить доступ к хосту
host-exec uname -a
```

---

## 📊 Текущая конфигурация

### Файлы:
- ✅ `/home/claude/workspace/docker-compose.yml` - обновлён
- ✅ `/home/claude/workspace/chatgpt-cli/Dockerfile` - обновлён
- ✅ `/home/claude/workspace/scripts/host-exec-nsenter.sh` - готов к использованию

### Backup:
- Резервная копия: `docker-compose.yml.backup-TIMESTAMP`

---

## 🆘 Откат изменений

Если нужно вернуться к предыдущей конфигурации:

```bash
cd /home/claude/workspace

# Восстановить из backup
sudo cp docker-compose.yml.backup-* docker-compose.yml

# Или удалить добавленные параметры вручную:
# - privileged
# - pid
# - cap_add
# - security_opt

# Пересобрать без sudo
sudo docker compose build chatgpt-cli
sudo docker compose restart chatgpt-cli
```

